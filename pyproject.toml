[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Tobin project test"
authors = [{name = "Tobin Frost", email = "habib92m@gmail.com"}]
dynamic = ["version", "description"]
